# 智能客服系统 - 启动指南

## 项目结构

```
LangchainChatFlow/
├── backend/                    # Python 后端 (Flask)
│   ├── app.py                 # Flask 主应用
│   ├── config.json            # 配置文件
│   ├── requirements.txt       # Python 依赖
│   ├── modules/               # 核心功能模块
│   │   ├── ai_client.py       # AI 客户端
│   │   ├── assistant.py       # AI 助手/Agent
│   │   ├── vector_stores/     # 向量存储
│   │   │   ├── base_vector_store.py  # 向量存储基类（含工具方法）
│   │   │   ├── chroma_store.py       # Chroma 实现
│   │   │   ├── milvus_store.py       # Milvus 实现
│   │   │   └── store_factory.py      # 存储工厂
│   │   ├── prompt/            # Prompt 模板（支持 FewShot）
│   │   │   └── __init__.py    # 包含 LengthBasedExampleSelector
│   │   ├── document_loaders/  # 文档加载器
│   │   └── tools/             # 工具插件
│   ├── knowledge_base/       # 知识库文档
│   └── db/                    # 向量数据库
│
└── frontend/                   # React 前端 (Vite)
    ├── src/
    │   ├── components/         # UI 组件
    │   │   ├── ChatArea.jsx   # 聊天区域
    │   │   ├── Header.jsx    # 顶部标题栏
    │   │   └── InputArea.jsx  # 输入区域
    │   └── api/
    │       └── chat.js        # API 调用
    └── package.json
```

## 启动指南

### 1. 启动后端服务

```bash
cd backend
pip install -r requirements.txt
python app.py
```

后端运行在: `http://localhost:5000`

### 2. 启动前端服务

```bash
cd frontend
npm install
npm run dev
```

前端运行在: `http://localhost:5173`

### 3. 访问应用

浏览器打开: `http://localhost:5173`

## 功能说明

- **顶部**: 显示"智能客服"标题
- **中间**: 聊天内容区域
  - 左侧: 客服回复（白色气泡）
  - 右侧: 用户消息（紫色渐变气泡）
- **底部**: 输入框和发送按钮
  - 支持回车发送
  - 支持点击按钮发送

## 初始状态

进入应用后，会自动显示欢迎消息：
> "您好！我是智能客服，有什么可以帮助您的吗？"

## API 接口

- `GET /start` - 检查服务状态
- `POST /chat` - 发送对话请求

## 技术栈

**后端:**
- Flask 3.0.0 - Web 框架
- Flask-CORS 4.0.0 - 跨域支持
- OpenAI SDK >= 1.40.0 - AI 对话（兼容阿里云百炼）
- LangChain >= 0.3.0 - Agent、工具框架、ExampleSelector
- langchain-chroma >= 0.1.0 - Chroma 向量存储集成
- langchain-community >= 0.3.0 - 社区工具和嵌入模型
- dashscope >= 1.20.0 - 阿里云百炼 Embedding 支持
- Chroma >= 0.5.0 - 向量数据库

**前端:**
- React 18 - UI 框架
- Vite - 构建工具
- Axios - HTTP 请求

## 工具系统

系统支持以下工具调用：

1. **天气查询** (`get_weather`) - 查询指定城市的实时天气
2. **知识库检索** (`retrieve_knowledge`) - 从知识库检索相关信息
3. **表单提交** (`submit_form`) - 提交表单数据

工具调用规则：
- 实时天气问题优先调用 `get_weather`
- 知识类问题调用 `retrieve_knowledge`
- Agent 自动判断并选择合适的工具

## 自定义配置

### 修改系统提示词

编辑 `backend/modules/prompt/__init__.py` 中的 `CUSTOMER_SERVICE_PROMPT_TEMPLATE` 变量

### 自定义 FewShot 示例

编辑 `backend/modules/prompt/__init__.py` 中的 `DEFAULT_FEW_SHOT_EXAMPLES` 列表
