# 智能客服后端服务运行流程分析

## 整体架构概览

这是一个基于 Flask + OpenAI API（阿里云百炼）的智能客服后端系统，采用模块化设计，支持知识库检索（RAG）、工具调用和多轮对话。系统已迁移至 **MCP（Model Context Protocol）** 架构，支持工具的独立部署和多 Agent 共享调用。

---

## 完整运行流程

### 阶段一：启动 MCP 服务器 (`mcp_module/start.py`)

```
启动命令: python mcp_module/start.py
```

#### 1. 加载工具模块
- 静态导入工具插件（触发装饰器注册）
- 工具通过 `@register_tool` 装饰器注册到全局注册表 `_TOOL_REGISTRY`

#### 2. 注册工具到 MCP 服务器
- 从注册表获取所有已注册的工具
- 使用 `FastMCP.tool()` 方法注册到 MCP 服务器

#### 3. 启动 MCP 服务器
- 监听 `http://0.0.0.0:8080/mcp`
- 支持 Streamable HTTP 协议

---

### 阶段二：系统启动初始化 (`app.py` → `init_system()`)

```
启动命令: python app.py
```

#### 1. 加载环境变量
- 读取 `.env` 文件中的敏感配置（API Key 等）
- 使用 `python-dotenv` 库加载

#### 2. 初始化知识库 (`modules/vector_stores/`)

**流程：**
```
检查 db/ 目录是否存在向量数据
    ↓
如果不存在或为空：
    1. 读取知识库文档 (knowledge_base/)
    2. 按段落分割知识内容
    3. 调用阿里云百炼 API 生成向量 (text-embedding-v3 模型)
    4. 使用 Chroma 向量数据库存储
    ↓
如果已存在：
    跳过向量化，直接加载
```

**关键文件：**
- `knowledge_base/` - 原始知识文档目录
- `modules/vector_stores/chroma_store.py` - 向量存储管理模块

#### 3. 初始化 AI 助手 (`modules/assistant.py`)

**流程：**
```
1. 读取 config.json (API 配置)
2. 读取 prompt 模板
3. 创建 OpenAI 客户端（连接阿里云百炼）
4. 从 MCP 服务器获取工具列表
5. 初始化 Agent 实例
```

**关键配置：**
- `config.json` - API Key、Base URL、模型名称、向量存储配置
- `modules/prompt/__init__.py` - 系统指令模板（含 FewShot 示例）
- `mcp_module/mcp_service.py` - MCP 工具服务客户端

---

### 阶段三：API 服务运行 (`app.py` → `app.run()`)

启动 Flask 服务，监听 `http://localhost:5000`

#### 接口 1: GET /start - 服务状态检查

**流程：**
```
前端请求: GET http://localhost:5000/start
    ↓
后端检查：
    1. AI 模型是否就绪
    2. 知识库是否加载成功
    ↓
返回 JSON:
{
  "status": "ready",
  "message": "客服系统已就绪 (LangChain)",
  "model": "qwen-plus",
  "knowledge_base": true
}
```

---

#### 接口 2: POST /chat - 核心对话接口

**完整对话流程：**

```
前端发送 POST /chat
    ↓
接收用户消息和 session_id
    ↓
MCPToolService.get_tools() 从 MCP 服务器获取工具列表
    ↓
RAG 增强查询（从知识库检索相关内容）
    ↓
获取或创建会话历史
    ↓
添加系统指令 + 用户消息
    ↓
调用阿里云百炼 Chat API
    ↓
判断：是否需要调用工具？
    ↓
├─ 否 → 直接返回 AI 回复
└─ 是 → 解析工具调用信息
        ↓
        通过 MCP 客户端调用远程工具
        ↓
        将工具结果返回给 AI
        ↓
        AI 生成最终回复
        ↓
        返回给前端
```

---

## 详细流程拆解

### 流程 1: 普通对话（无工具调用）

```python
# 1. 前端请求
POST /chat
{
  "message": "你们的产品有什么功能？",
  "session_id": "abc-123"
}

# 2. 后端处理
app.py → chat() 函数
    ↓
# 2.1 RAG 增强查询
vector_store_instance.init_knowledge_base()
assistant.py → process_message(session_id, user_message)
    ↓
a. 获取会话历史（包含系统指令）
   messages = [
     {"role": "system", "content": "你是智能客服..."},
     {"role": "user", "content": "你们的产品有什么功能？"}
   ]
    ↓
b. 调用阿里云百炼 API
   response = client.chat.completions.create(
       model="qwen-plus",
       messages=messages,
       tools=工具定义,
       tool_choice="auto"
   )
    ↓
c. 获取 AI 回复
   AI 判断：这是普通问题，不需要调用工具
   直接生成文本回复
    ↓
d. 保存到会话历史
    ↓
e. 返回给前端
{
  "reply": "我们的产品具有以下功能...",
  "tool_calls": [],
  "session_id": "abc-123",
  "finished": false
}
```

### 流程 2: 工具调用对话（天气查询）

```python
# 1. 前端请求
POST /chat
{
  "message": "杭州天气怎么样？",
  "session_id": "abc-123"
}

# 2. 后端处理
app.py → chat() 函数
    ↓
assistant.py → process_message()
    ↓
a. AI 判断需要查询天气，返回工具调用
   assistant_message.tool_calls = [
     {
       "id": "call_123",
       "function": {
         "name": "get_weather",
         "arguments": '{"city":"杭州"}'
       }
     }
   ]
    ↓
b. LangChain AgentExecutor 自动执行工具
   通过 MCPToolService 调用远程 MCP 服务器
   mcp_service.py → call_tool()
    ↓
c. MCP 服务器执行工具
   mcp_module/tools/weather_plugin.py → get_weather()
   调用 wttr.in API 获取天气数据
   返回结果给应用服务器
    ↓
d. 将工具结果提交给 AI
   发送第二轮 API 请求:
   messages = [
     {"role": "system", "content": "..."},
     {"role": "user", "content": "杭州天气怎么样？"},
     {"role": "assistant", "tool_calls": [...]},
     {"role": "tool", "tool_call_id": "call_123", "content": "..."}
   ]
    ↓
e. AI 根据工具执行结果生成最终回复
    ↓
f. 返回给前端
{
  "reply": "杭州今天天气晴朗，温度25°C，适合户外活动...",
  "tool_calls": [{"id": "call_123", "name": "get_weather", ...}],
  "session_id": "abc-123",
  "finished": false
}
```

### 流程 3: 链式工具调用（天气推荐）

```python
# 1. 前端请求
POST /chat
{
  "message": "杭州天气怎么样？适合去哪儿玩？",
  "session_id": "abc-123"
}

# 2. 后端处理
# AI 自动进行链式调用：
# 1. 先调用 get_weather 获取天气
# 2. 再调用 get_weather_forecast 获取推荐

assistant.py → process_message()
    ↓
a. AI 调用 get_weather 工具（通过 MCP）
    ↓
b. 获取天气结果后，AI 自动调用 get_weather_forecast（通过 MCP）
    ↓
c. AI 生成综合回复
    ↓
d. 返回给前端
{
  "reply": "杭州今天天气晴朗，温度25°C。推荐您去西湖游玩...",
  "tool_calls": [...],
  "session_id": "abc-123",
  "finished": false
}
```

---

## MCP 架构说明

### MCP 服务器组件

| 组件 | 文件 | 功能说明 |
|------|------|----------|
| **配置管理** | `mcp_module/config.py` | MCP 服务器端口、路径等配置常量 |
| **日志模块** | `mcp_module/logger.py` | 统一日志记录功能 |
| **服务器核心** | `mcp_module/mcp_server.py` | FastMCP 服务器初始化和工具注册 |
| **客户端** | `mcp_module/mcp_client.py` | MCP 客户端实现，用于获取工具列表 |
| **服务封装** | `mcp_module/mcp_service.py` | MCP 服务封装，提供统一工具获取接口 |
| **工具注册中心** | `mcp_module/tools/registry.py` | 工具注册装饰器和全局注册表 |
| **工具插件** | `mcp_module/tools/*.py` | 具体工具实现（天气、表单等） |

### 工具注册机制

```python
# 工具插件定义
from mcp_module.tools.registry import register_tool

@register_tool(
    name="get_weather",
    description="查询指定城市的实时天气",
    parameters=[
        {"name": "city", "type": "string", "description": "城市名称", "required": True}
    ],
    return_type="string"
)
def get_weather(city: str) -> str:
    # 工具实现
    pass

# 启动时自动注册
# mcp_module/start.py 导入工具模块触发装饰器执行
```

---

## 模块说明

### modules/ai_client.py
AI 客户端管理，封装与阿里云百炼 API 的交互

### modules/assistant.py
AI 助手/Agent，负责处理对话流程和工具调用

### modules/vector_stores/
向量数据库管理，负责知识库的向量化存储和检索

### modules/prompt/__init__.py
Prompt 模板管理，定义系统指令和 FewShot 示例

### mcp_module/
MCP 模块，提供工具服务的独立部署能力
- `config.py` - 配置常量
- `logger.py` - 统一日志
- `mcp_server.py` - MCP 服务器
- `mcp_client.py` - MCP 客户端
- `mcp_service.py` - 服务封装
- `tools/` - 工具插件目录