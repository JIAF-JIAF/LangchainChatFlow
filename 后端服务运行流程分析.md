# 智能客服后端服务运行流程分析

## 整体架构概览

这是一个基于 Flask + OpenAI API（阿里云百炼）的智能客服后端系统，采用模块化设计，支持知识库检索（RAG）、工具调用和多轮对话。

---

## 完整运行流程

### 阶段一：系统启动初始化 (`app.py` → `init_system()`)

```
启动命令: python app.py
```

#### 1. 加载环境变量
- 读取 `.env` 文件中的敏感配置（API Key 等）
- 使用 `python-dotenv` 库加载

#### 2. 初始化知识库 (`modules/store/vector_store.py`)

**流程：**
```
检查 db/ 目录是否存在向量数据
    ↓
如果不存在或为空：
    1. 读取知识库文档 (knowledge_base/)
    2. 按段落分割知识内容
    3. 调用阿里云百炼 API 生成向量 (text-embedding-v3 模型)
    4. 使用 Chroma 向量数据库存储
    ↓
如果已存在：
    跳过向量化，直接加载
```

**关键文件：**
- `knowledge_base/` - 原始知识文档目录
- `modules/store/vector_store.py` - 向量存储管理模块

#### 3. 初始化 AI 助手 (`modules/assistant.py`)

**流程：**
```
1. 读取 config.json (API 配置)
2. 读取 prompt 模板
3. 创建 OpenAI 客户端（连接阿里云百炼）
4. 初始化 Agent 实例
5. 加载工具插件 (modules/tools/)
```

**关键配置：**
- `config.json` - API Key、Base URL、模型名称
- `modules/prompt/__init__.py` - 系统指令模板
- `modules/tools/` - 工具插件目录

---

### 阶段二：API 服务运行 (`app.py` → `app.run()`)

启动 Flask 服务，监听 `http://localhost:5000`

#### 接口 1: GET /start - 服务状态检查

**流程：**
```
前端请求: GET http://localhost:5000/start
    ↓
后端检查：
    1. AI 模型是否就绪
    2. 知识库是否加载成功
    ↓
返回 JSON:
{
  "status": "ready",
  "message": "客服系统已就绪 (LangChain)",
  "model": "qwen-plus",
  "knowledge_base": true
}
```

---

#### 接口 2: POST /chat - 核心对话接口

**完整对话流程：**

```
前端发送 POST /chat
    ↓
接收用户消息和 session_id
    ↓
RAG 增强查询（从知识库检索相关内容）
    ↓
获取或创建会话历史
    ↓
添加系统指令 + 用户消息
    ↓
调用阿里云百炼 Chat API
    ↓
判断：是否需要调用工具？
    ↓
├─ 否 → 直接返回 AI 回复
└─ 是 → 解析工具调用信息
        ↓
        执行相应工具（天气查询、天气推荐或表单提交）
        ↓
        将工具结果返回给 AI
        ↓
        AI 生成最终回复
        ↓
        返回给前端
```

---

## 详细流程拆解

### 流程 1: 普通对话（无工具调用）

```python
# 1. 前端请求
POST /chat
{
  "message": "你们的产品有什么功能？",
  "session_id": "abc-123"
}

# 2. 后端处理
app.py → chat() 函数
    ↓
# 2.1 RAG 增强查询
vector_store_instance.init_knowledge_base()
assistant.py → process_message(session_id, user_message)
    ↓
a. 获取会话历史（包含系统指令）
   messages = [
     {"role": "system", "content": "你是智能客服..."},
     {"role": "user", "content": "你们的产品有什么功能？"}
   ]
    ↓
b. 调用阿里云百炼 API
   response = client.chat.completions.create(
       model="qwen-plus",
       messages=messages,
       tools=工具定义,
       tool_choice="auto"
   )
    ↓
c. 获取 AI 回复
   AI 判断：这是普通问题，不需要调用工具
   直接生成文本回复
    ↓
d. 保存到会话历史
    ↓
e. 返回给前端
{
  "reply": "我们的产品具有以下功能...",
  "tool_calls": [],
  "session_id": "abc-123",
  "finished": false
}
```

### 流程 2: 工具调用对话（天气查询）

```python
# 1. 前端请求
POST /chat
{
  "message": "杭州天气怎么样？",
  "session_id": "abc-123"
}

# 2. 后端处理
app.py → chat() 函数
    ↓
assistant.py → process_message()
    ↓
a. AI 判断需要查询天气，返回工具调用
   assistant_message.tool_calls = [
     {
       "id": "call_123",
       "function": {
         "name": "get_weather",
         "arguments": '{"city":"杭州"}'
       }
     }
   ]
    ↓
b. app.py 检测到 tool_calls，开始执行工具
   for tool_call in result['tool_calls']:
       tool_result = execute_tool(tool_call)
    ↓
c. modules/tools/weather_plugin.py → _handle_get_weather()
   调用 wttr.in API 获取天气数据
    ↓
d. 将工具结果提交给 AI
   发送第二轮 API 请求:
   messages = [
     {"role": "system", "content": "..."},
     {"role": "user", "content": "杭州天气怎么样？"},
     {"role": "assistant", "tool_calls": [...]},
     {"role": "tool", "tool_call_id": "call_123", "content": "..."}
   ]
    ↓
e. AI 根据工具执行结果生成最终回复
    ↓
f. 返回给前端
{
  "reply": "杭州今天天气晴朗，温度25°C，适合户外活动...",
  "tool_calls": [{"id": "call_123", "name": "get_weather", ...}],
  "session_id": "abc-123",
  "finished": false
}
```

### 流程 3: 链式工具调用（天气推荐）

```python
# 1. 前端请求
POST /chat
{
  "message": "杭州天气怎么样？适合去哪儿玩？",
  "session_id": "abc-123"
}

# 2. 后端处理
# AI 自动进行链式调用：
# 1. 先调用 get_weather 获取天气
# 2. 再调用 get_weather_recommend 获取推荐

assistant.py → process_message()
    ↓
a. AI 调用 get_weather 工具
    ↓
b. 获取天气结果后，AI 自动调用 get_weather_recommend
    ↓
c. AI 生成综合回复
    ↓
d. 返回给前端
{
  "reply": "杭州今天天气晴朗，温度25°C。推荐您去西湖游玩...",
  "tool_calls": [...],
  "session_id": "abc-123",
  "finished": false
}
```

---

## 模块说明

### modules/ai_client.py
AI 客户端管理，封装与阿里云百炼 API 的交互

### modules/assistant.py
AI 助手/Agent，负责处理对话流程和工具调用

### modules/store/vector_store.py
向量数据库管理，负责知识库的向量化存储和检索

### modules/prompt/__init__.py
Prompt 模板管理，定义系统指令

### modules/tools/
工具插件目录
- `weather_plugin.py` - 天气查询工具
- `weather_recommend_plugin.py` - 天气推荐工具
- `submit_form_plugin.py` - 表单提交工具
