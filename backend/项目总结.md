# 智能客服 Agent - 项目总结

## 已完成功能

### 1. 核心架构
- Flask Web 服务框架
- AI 服务 API 集成（兼容 OpenAI SDK）
- 模块化设计，易于扩展
- 统一 AI 客户端管理 (AIClient)
- **MCP（Model Context Protocol）架构** - 工具独立部署，支持多 Agent 共享调用

### 2. 模块化 RAG 框架
- 文档索引模块 (Indexer) - 支持 Chroma、Milvus
- 文档检索模块 (Retriever) - 支持简单向量检索、重排序检索、过滤检索
- 回答生成模块 (Generator) - 支持 Stuff、Map-Reduce、Refine 策略
- 对话记忆模块 (Memory) - 支持 ConversationMemory
- 路由决策模块 (Router) - 决定是否检索及选择策略
- 自动检测知识库，向量为空时自动重新向量化
- 首次运行自动向量化原始文本（支持 .txt、.pdf、.docx）
- 基于余弦相似度的内容检索
- Chroma 向量数据库支持（兼容 langchain-chroma）
- 向量化失败安全处理

### 3. AI 助手模块
- AI 助手初始化和管理 (assistant.py)
- 自动创建/加载助手
- 会话线程管理
- 对话上下文维护
- 通过 MCP 服务器调用工具

### 4. 工具系统（MCP 架构）
- MCP 工具注册机制（通过 `@register_tool` 装饰器）
- 工具独立部署在 MCP 服务器
- 支持 Streamable HTTP 协议
- 支持多个 MCP 服务器配置
- 天气查询工具 (get_weather)
- 天气预报工具 (get_weather_forecast)
- 表单提交工具 (submit_form)
- 可扩展的工具接口

### 5. FewShot 学习系统
- LengthBasedExampleSelector 动态示例选择
- 基于输入长度自动裁剪示例
- 默认示例对话库 (DEFAULT_FEW_SHOT_EXAMPLES)
- 可自定义的 FewShot 示例配置

### 6. Web API
- GET /start - 服务状态检查
- POST /chat - 对话接口
- 会话管理(session_id)
- 限流支持（Flask-Limiter）
- 错误处理和状态码

### 7. 配置管理
- .env 文件支持
- config.json - 系统配置
- mcp_module/config.py - MCP 服务器配置
- 模块化配置读取
- 支持多个 MCP 服务器配置

### 8. 日志系统
- 统一日志模块 (mcp_module/logger.py)
- 结构化日志输出
- 支持不同日志级别

### 9. 限流模块
- Flask-Limiter 集成
- 可配置的限流规则
- 429 错误处理

### 10. 文档
- README.md - 项目说明
- 启动指南.md - 快速入门
- 后端服务运行流程分析.md - 架构分析
- 项目总结.md - 本文件

## 项目结构

```
backend/
├── app.py                    # Flask 主应用
├── requirements.txt          # Python 依赖
├── modules/                  # 核心功能模块
│   ├── __init__.py          # 模块包初始化
│   ├── ai_client.py          # AI 客户端
│   ├── assistant.py          # AI 助手/Agent
│   ├── rag/                  # 模块化 RAG 框架
│   │   ├── __init__.py
│   │   ├── rag_chain.py       # RAG 链核心
│   │   ├── indexer/           # 索引模块
│   │   │   ├── base.py        # 索引器基类
│   │   │   ├── chroma.py      # Chroma 实现
│   │   │   └── milvus.py      # Milvus 实现
│   │   ├── retriever/         # 检索模块
│   │   │   ├── base.py        # 检索器基类
│   │   │   ├── simple.py      # 简单向量检索
│   │   │   ├── reranking.py   # 重排序检索
│   │   │   └── filtered.py    # 过滤检索
│   │   ├── generator/         # 生成模块
│   │   │   ├── base.py        # 生成器基类
│   │   │   ├── stuff.py       # Stuff 策略
│   │   │   ├── map_reduce.py  # Map-Reduce 策略
│   │   │   └── refine.py      # Refine 策略
│   │   ├── memory/            # 记忆模块
│   │   │   ├── base.py        # 记忆基类
│   │   │   └── conversation.py# 对话记忆
│   │   └── router/            # 路由模块
│   │       ├── base.py        # 路由基类
│   │       └── simple.py      # 简单路由
│   ├── document_loaders/      # 文档加载器
│   │   ├── __init__.py
│   │   ├── loader_factory.py
│   │   ├── text_loader.py
│   │   ├── pdf_loader.py
│   │   └── docx_loader.py
│   ├── vector_stores/         # 向量存储（兼容旧接口）
│   │   ├── __init__.py
│   │   ├── base_vector_store.py  # 向量存储基类（含工具方法）
│   │   ├── chroma_store.py       # Chroma 实现
│   │   ├── milvus_store.py       # Milvus 实现
│   │   └── store_factory.py      # 存储工厂
│   ├── prompt/               # Prompt 模板（支持 FewShot）
│   │   └── __init__.py       # Prompt 配置、LengthBasedExampleSelector
│   └── rate_limit/           # 限流模块
│       ├── __init__.py
│       ├── langchain.py
│       └── rate_limiter.py
├── mcp_module/               # MCP 模块（工具服务）
│   ├── __init__.py          # MCP 模块初始化
│   ├── config.py            # MCP 配置常量
│   ├── logger.py            # 统一日志模块
│   ├── mcp_server.py        # MCP 服务器核心
│   ├── mcp_client.py        # MCP 客户端
│   ├── mcp_service.py       # MCP 服务封装
│   ├── start.py             # MCP 服务器启动脚本
│   └── tools/               # 工具插件目录
│       ├── __init__.py
│       ├── registry.py       # 工具注册中心
│       ├── weather_plugin.py          # 天气查询
│       ├── weather_recommend_plugin.py # 天气推荐
│       └── submit_form_plugin.py      # 表单提交
├── knowledge_base/           # 知识库文档
│   └── raw_knowledge.txt     # 知识库原文
└── db/                       # 向量数据库存储
```

## 核心功能说明

### 工作流程

1. **启动阶段**
   - **启动 MCP 服务器**: `python mcp_module/start.py`
   - 工具通过 `@register_tool` 装饰器自动注册到注册表
   - FastMCP 服务器启动，监听 `http://localhost:8080/mcp`
   - **启动应用服务器**: `python app.py`
   - 加载配置文件（.env 或 config.json）
   - 初始化 AI 客户端 (AIClient)
   - 初始化模块化 RAG 链
   - 构建知识库索引（自动检测并向量化）
   - 从 MCP 服务器获取工具列表
   - 创建/加载 AI 助手
   - 启动 Flask 服务

2. **对话阶段**
   - 接收用户消息
   - 创建/获取会话线程
   - 从 MCP 服务器获取工具列表
   - 调用 AI 助手处理
   - RAG 检索增强（Router 判断 → Retriever 检索）
   - 检测工具调用需求
   - 通过 MCP 客户端调用远程工具
   - 返回 AI 回复

3. **工具调用流程**
   - AI 判断是否需要调用工具
   - 通过 MCPToolService 向 MCP 服务器发送 HTTP 请求
   - MCP 服务器执行工具并返回结果
   - 将结果返回 AI
   - AI 生成最终回复

### FewShot 学习流程

1. **初始化阶段**
   - 加载 DEFAULT_FEW_SHOT_EXAMPLES 示例列表
   - 创建 LengthBasedExampleSelector 实例
   - 将示例注入到系统提示模板

2. **对话阶段**
   - 根据输入长度选择合适的示例
   - 将示例拼接到系统提示中
   - 发送给 AI 模型处理

### MCP 架构流程

```
┌─────────────────────────────────────────────────────┐
│                MCP 服务器 (8080端口)                 │
│  ┌─────────────────────────────────────────────┐   │
│  │           工具注册表 (_TOOL_REGISTRY)          │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │get_weather│ │get_weather_forecast│ │submit_form│ │   │
│  │  └────┬────┘ └────┬────┘ └──────┬──────┘   │   │
│  └───────┼───────────┼─────────────┼───────────┘   │
│          │           │             │               │
│          └───────────┼─────────────┘               │
│                      ↓                             │
│            ┌─────────────────┐                     │
│            │  FastMCP Server │ ← Streamable HTTP   │
│            └─────────────────┘                     │
└─────────────────────────────────────────────────────┘
                          ↑
                          │ HTTP 请求
                          ↓
┌─────────────────────────────────────────────────────┐
│              应用服务器 (5000端口)                    │
│  ┌─────────────┐    ┌─────────────┐                │
│  │   Agent     │←───│MCPToolService│                │
│  │             │    │   (客户端)   │                │
│  └─────────────┘    └─────────────┘                │
└─────────────────────────────────────────────────────┘
```

## API 接口

#### GET /start
检查服务状态

**请求:**
```
GET http://localhost:5000/start
```

**响应:**
```json
{
  "status": "ready",
  "message": "客服系统已就绪 (LangChain)",
  "model": "qwen-plus",
  "knowledge_base": true
}
```

#### POST /chat
处理对话请求

**请求:**
```json
{
  "message": "用户输入",
  "session_id": "可选会话ID"
}
```

**响应:**
```json
{
  "reply": "AI回复内容",
  "tool_calls": [],
  "session_id": "会话ID",
  "finished": false
}
```

## 技术栈

- **后端框架**: Flask 3.0.0
- **AI SDK**: OpenAI >= 1.40.0（兼容阿里云百炼）
- **向量化**: text-embedding-v3
- **AI 模型**: qwen-plus / qwen3.6-flash
- **向量数据库**: Chroma >= 0.5.0
- **LangChain**: >= 0.3.0 (Agent、工具框架、ExampleSelector)
- **MCP**: Model Context Protocol（工具服务协议）
- **限流**: Flask-Limiter 3.5.0
- **HTTP 客户端**: requests 2.31.0
- **数值计算**: numpy 2.4.4

## 配置说明

### config.json

```json
{
  "api_key": "your_api_key",
  "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1",
  "model": "qwen-plus",
  "embedding_model": "text-embedding-v3"
}
```

### MCP 配置 (mcp_module/config.py)

```python
# MCP 服务器配置
MCP_HOST = "0.0.0.0"
MCP_PORT = 8080
MCP_PATH = "/mcp"
MCP_URL = f"http://127.0.0.1:{MCP_PORT}{MCP_PATH}"

# 多个 MCP 服务器配置
MCP_SERVERS = [
    {
        "name": "default",
        "url": MCP_URL,
        "enabled": True,
        "description": "默认 MCP 服务器"
    }
]

# 应用服务器配置
APP_HOST = "0.0.0.0"
APP_PORT = 5000
```

## 使用方法

### 1. 安装依赖
```bash
cd backend
pip install -r requirements.txt
```

### 2. 配置 API Key
编辑 `config.json` 文件，填入 API Key

### 3. 准备知识库
编辑 `knowledge_base/raw_knowledge.txt`，添加业务知识

### 4. 启动服务

**启动 MCP 服务器（工具服务）:**
```bash
python mcp_module/start.py
```

**启动应用服务器:**
```bash
python app.py
```

### 5. 测试
```bash
# 使用 curl 测试
curl -X POST http://localhost:5000/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "杭州天气怎么样？"}'
```

## 自定义扩展

### 修改系统提示词

编辑 `modules/prompt/__init__.py` 中的 `CUSTOMER_SERVICE_PROMPT_TEMPLATE` 变量

### 自定义 FewShot 示例

编辑 `modules/prompt/__init__.py` 中的 `DEFAULT_FEW_SHOT_EXAMPLES` 列表

### 添加新工具

1. 在 `mcp_module/tools/` 创建插件文件
2. 使用 `@register_tool` 装饰器注册工具
3. 在 `mcp_module/start.py` 中导入工具模块
4. 重启 MCP 服务

```python
from mcp_module.tools.registry import register_tool

@register_tool(
    name="my_tool",
    description="我的自定义工具",
    parameters=[
        {"name": "param1", "type": "string", "description": "参数说明", "required": True}
    ],
    return_type="string"
)
def my_tool(param1: str) -> str:
    return "工具执行结果"
```

### 更新知识库

1. 在 `knowledge_base/` 修改文档
2. 重启服务自动重新向量化

## 已修复的问题

- ✅ MCP 架构迁移完成
- ✅ 工具独立部署支持
- ✅ Streamable HTTP 协议支持
- ✅ 统一日志模块
- ✅ 集中配置管理
- ✅ 模块化 RAG 框架
- ✅ 限流模块集成
- ✅ 工具注册机制优化
- ✅ 工具格式与 LangChain 兼容
- ✅ 闭包变量问题修复
- ✅ 参数格式兼容处理
- ✅ 端口冲突处理