# 智能客服 Agent - 快速启动指南

## 前置准备

### 1. 获取 API Key

访问 DeepSeek 平台获取 API Key：
- DeepSeek: https://platform.deepseek.com/

### 2. 配置环境变量

**使用 config.json 文件**

```json
{
  "api_key": "your_api_key",
  "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1",
  "model": "qwen-plus",
  "embedding_model": "text-embedding-v3"
}
```

| 配置项 | 说明 |
|--------|------|
| api_key | 阿里云百炼或 OpenAI API 密钥 |
| base_url | API 基础地址 |
| model | 对话模型名称 |
| embedding_model | 向量化模型名称 |

## 启动服务

### 方式一: 完整启动（推荐）

**步骤 1: 启动 MCP 服务器（工具服务）**

打开终端，进入 backend 目录：

```bash
cd backend
python mcp_module/start.py
```

MCP 服务器运行在: `http://localhost:8080/mcp`

**步骤 2: 启动应用服务器**

打开另一个终端，进入 backend 目录：

```bash
cd backend
python app.py
```

后端运行在: `http://localhost:5000`

### 方式二: 使用测试脚本

终端1 启动 MCP 服务:
```bash
cd backend
python mcp_module/start.py
```

终端2 启动应用服务:
```bash
cd backend
python app.py
```

终端3 运行测试:
```bash
cd backend
python test.py
```

## API 测试

### 1. 检查服务状态

浏览器访问: http://localhost:5000/start

或使用 curl:
```bash
curl http://localhost:5000/start
```

**响应示例:**
```json
{
  "status": "ready",
  "message": "客服系统已就绪 (LangChain)",
  "model": "qwen-plus",
  "knowledge_base": true
}
```

### 2. 发送对话请求

使用 curl:
```bash
curl -X POST http://localhost:5000/chat \
  -H "Content-Type: application/json" \
  -d "{\"message\": \"你好，我想了解一下你们的产品\"}"
```

使用 Python:
```python
import requests

response = requests.post(
    'http://localhost:5000/chat',
    json={'message': '你好，我想了解一下你们的产品'}
)
print(response.json())
```

### 3. 测试工具调用

```bash
# 查询天气
curl -X POST http://localhost:5000/chat \
  -H "Content-Type: application/json" \
  -d "{\"message\": \"杭州天气怎么样？\"}"
```

## MCP 架构说明

### 架构图

```
┌─────────────────────────────────────────────────────┐
│                MCP 服务器 (8080端口)                 │
│  ┌─────────────────────────────────────────────┐   │
│  │           工具注册表 (_TOOL_REGISTRY)          │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │   │
│  │  │get_weather│ │get_weather_forecast│ │submit_form│ │   │
│  │  └────┬────┘ └────┬────┘ └──────┬──────┘   │   │
│  └───────┼───────────┼─────────────┼───────────┘   │
│          │           │             │               │
│          └───────────┼─────────────┘               │
│                      ↓                             │
│            ┌─────────────────┐                     │
│            │  FastMCP Server │ ← Streamable HTTP   │
│            └─────────────────┘                     │
└─────────────────────────────────────────────────────┘
                          ↑
                          │ HTTP 请求
                          ↓
┌─────────────────────────────────────────────────────┐
│              应用服务器 (5000端口)                    │
│  ┌─────────────┐    ┌─────────────┐                │
│  │   Agent     │←───│MCPToolService│                │
│  │             │    │   (客户端)   │                │
│  └─────────────┘    └─────────────┘                │
└─────────────────────────────────────────────────────┘
```

### MCP 服务器日志

MCP 服务器启动后会输出如下日志：

```
2026-05-08 19:29:01 - chartflow - INFO - 从注册表注册工具...
2026-05-08 19:29:01 - chartflow - INFO - 注册工具: get_weather
2026-05-08 19:29:01 - chartflow - INFO - 注册工具: get_weather_forecast
2026-05-08 19:29:01 - chartflow - INFO - 注册工具: submit_form
2026-05-08 19:29:01 - chartflow - INFO - 工具注册完成
2026-05-08 19:29:01 - chartflow - INFO - 启动服务器...
2026-05-08 19:29:01 - chartflow - INFO - 启动 MCP 服务器 (streamable-http): http://0.0.0.0:8080/mcp
```

## 自定义配置

### 修改知识库

编辑 `knowledge_base/` 目录下的文件，添加业务知识。

### 修改系统提示词

编辑 `modules/prompt/__init__.py` 中的 `CUSTOMER_SERVICE_PROMPT_TEMPLATE` 常量

### 自定义 FewShot 示例

编辑 `modules/prompt/__init__.py` 中的 `DEFAULT_FEW_SHOT_EXAMPLES` 列表：

```python
DEFAULT_FEW_SHOT_EXAMPLES = [
    {"user_query": "你好", "assistant_response": "您好！请问有什么可以帮助您的？"},
    {"user_query": "你们有什么产品?", "assistant_response": "我们提供多种优质产品..."},
]
```

### 添加新工具

1. 在 `mcp_module/tools/` 创建插件文件
2. 使用 `@register_tool` 装饰器注册工具
3. 在 `mcp_module/start.py` 中导入工具模块
4. 重启 MCP 服务器

```python
from mcp_module.tools.registry import register_tool

@register_tool(
    name="my_tool",
    description="我的自定义工具",
    parameters=[
        {"name": "param1", "type": "string", "description": "参数说明", "required": True}
    ],
    return_type="string"
)
def my_tool(param1: str) -> str:
    return "工具执行结果"
```

## 常见问题

**Q: 启动时提示 API Key 错误?**
A: 检查 `.env` 或 `config.json` 文件是否正确配置了 `api_key`

**Q: 向量化失败?**
A: 检查网络连接，确认 API Key 有 embedding 权限

**Q: 如何重置知识库?**
A: 删除 `db/` 目录下的向量数据文件，重启服务即可

**Q: MCP 服务器端口被占用?**
A: 使用 `taskkill /F /PID <PID>` 终止占用进程，或等待端口释放

**Q: 工具调用失败?**
A: 检查 MCP 服务器是否正常运行，确保应用能访问 `http://localhost:8080/mcp`

## 项目文件说明

```
backend/
├── app.py                    # 主程序入口
├── requirements.txt          # Python 依赖
├── modules/                  # 核心功能模块
│   ├── __init__.py          # 模块包初始化
│   ├── ai_client.py         # AI 客户端
│   ├── assistant.py          # AI 助手/Agent
│   ├── rag/                  # 模块化 RAG 框架
│   │   ├── rag_chain.py       # RAG 链核心
│   │   ├── indexer/           # 索引模块
│   │   ├── retriever/         # 检索模块
│   │   ├── generator/         # 生成模块
│   │   ├── memory/            # 记忆模块
│   │   └── router/            # 路由模块
│   ├── document_loaders/      # 文档加载器
│   │   ├── loader_factory.py
│   │   ├── text_loader.py
│   │   ├── pdf_loader.py
│   │   └── docx_loader.py
│   ├── vector_stores/         # 向量存储
│   │   ├── base_vector_store.py  # 向量存储基类（含工具方法）
│   │   ├── chroma_store.py       # Chroma 实现
│   │   ├── milvus_store.py       # Milvus 实现
│   │   └── store_factory.py      # 存储工厂
│   ├── prompt/              # Prompt 模板（支持 FewShot）
│   │   └── __init__.py      # 包含 LengthBasedExampleSelector
│   └── rate_limit/          # 限流模块
│       ├── __init__.py
│       ├── langchain.py
│       └── rate_limiter.py
├── mcp_module/              # MCP 模块（工具服务）
│   ├── __init__.py          # MCP 模块初始化
│   ├── config.py            # MCP 配置常量
│   ├── logger.py            # 统一日志模块
│   ├── mcp_server.py        # MCP 服务器核心
│   ├── mcp_client.py        # MCP 客户端
│   ├── mcp_service.py       # MCP 服务封装
│   ├── start.py             # MCP 服务器启动脚本
│   └── tools/               # 工具插件目录
│       ├── registry.py       # 工具注册中心
│       ├── weather_plugin.py          # 天气查询
│       ├── weather_recommend_plugin.py # 天气推荐
│       └── submit_form_plugin.py      # 表单提交
├── knowledge_base/          # 知识库文档
└── db/                      # 向量数据存储
```

## 下一步

1. 配置 API Key
2. 启动 MCP 服务器
3. 启动应用服务器
4. 测试对话
5. 修改知识库内容
6. 自定义助手行为
7. 自定义 FewShot 示例
8. 扩展更多工具